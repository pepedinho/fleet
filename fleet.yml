ENV: &default_env
  SECRET_TOKEN: $
  TEST_KEY: "oui"

update:
  env: *default_env
  steps: 
    # - cmd: git pull
    # - cmd: env
    - cmd: cargo b
      container: rust:1.88
    - cmd: ping google.com
      blocking: true

on_conflict:
  env: *default_env
  steps: 
  - cmd: echo "un conflit est survenu"

post_update:
  - echo "update terminé"