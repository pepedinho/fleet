ENV: &default_env
  SECRET_TOKEN: $
  TEST_KEY: "oui"

update:
  env: *default_env
  steps: 
    - cmd: git pull
    - cmd: env
    - cmd: ping google.com
      blocking: true

on_confliot:
  - echo "un conflit est survenu"
  - git reset --hard origin/main

post_update:
  - echo "update terminé"