update:
  - cmd: echo "oui je test"
  - cmd: ping google.com
    blocking: true

on_confliot:
  - echo "un conflit est survenu"
  - git reset --hard origin/main

post_update:
  - echo "update terminé"